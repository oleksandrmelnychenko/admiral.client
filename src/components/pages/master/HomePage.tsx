import React from 'react';
import { TransitionWrapper } from '../../../helpers/transition.wrapper';

const HomePage: React.FC = () => {
  return (
    <TransitionWrapper>
      <div className="main">MAIN PAGE</div>
    </TransitionWrapper>
  );
};

export default HomePage;
